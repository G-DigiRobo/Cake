<%#ジャンル index まちい%>
<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-8'>
<h3>ジャンル一覧・追加</h3>
<%= form_with model: @genre, url: admin_genres_path do |f| %>
 <table class="table" style="border:none;">
  <thead style="border:none;">
    <tr>
      <th scope="col" style="border:none;">ジャンル名</th>
      <th scope="col" style="border:none;"><%= f.text_field :name %></th>
      <th scope="col" style="border:none;"><%= f.submit "新規登録", class:"btn btn-primary rounded-pill text-light" %></th>
    </tr>
  </thead>
</table>
<% end %>
<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th>ジャンル</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= genre.name %></td>
        <td>
          <%= link_to edit_admin_genre_path(genre.id),class:"btn btn-primary rounded-pill text-light" do %>
           編集する
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
    </div>
  </div>
</div>